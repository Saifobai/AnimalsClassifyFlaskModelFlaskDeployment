runtime: python-3.10
services:
  - type: web
    name: animal-classifier
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: FLASK_ENV
        value: production
